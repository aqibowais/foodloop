rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all read/write/list operations for authenticated users
    // Security is handled at the application level
    match /{document=**} {
      allow read, write, list: if request.auth != null;
    }
  }
}
